#include <stdio.h>

main()
{

int num;

    printf("Introduza um Nº ");         scanf("%d", &num);

    if (num == 0)
        printf("É igual a zero!");
    else 
        printf("É diferente de zero!");
}